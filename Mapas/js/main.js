function calcularVariable() {
    // Realiza cálculos y obtiene la variable
    var miVariable = 42; // Por ejemplo

    // Llama a una función en el servidor para actualizar el valor en el TextBox
    actualizarTextBox(miVariable);
}